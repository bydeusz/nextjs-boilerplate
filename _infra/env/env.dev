# Development Environment Variables
# This file is safe to commit as it contains only development values

# MySQL Configuration (for Headless CMS - WordPress)
MYSQL_ROOT_PASSWORD=root
MYSQL_DATABASE=headless
MYSQL_USER=root
MYSQL_PASSWORD=root
MYSQL_PORT=3306

# PostgreSQL Configuration (for Next.js App)
POSTGRES_DB=business
POSTGRES_USER=root
POSTGRES_PASSWORD=root
POSTGRES_PORT=5432

# MinIO Configuration
MINIO_ROOT_USER=minioadmin
MINIO_ROOT_PASSWORD=minioadmin
MINIO_ENDPOINT=localhost
MINIO_PORT=9000
MINIO_CONSOLE_PORT=9001
MINIO_USE_SSL=false

# Application Configuration
NODE_ENV=development
NEXT_PUBLIC_APP_URL=http://localhost:3000

# WordPress Configuration
WORDPRESS_DB_NAME=headless
WORDPRESS_PORT=8000
PHPMYADMIN_PORT=8080

# Qdrant Configuration
QDRANT_PORT=6333
QDRANT_GRPC_PORT=6334
QDRANT_API_KEY=qdrant_dev_key_123