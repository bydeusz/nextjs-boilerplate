# Development Environment Variables
# This file is safe to commit as it contains only development values

# MySQL Configuration (Single Database with Multiple Schemas)
MYSQL_ROOT_PASSWORD=root
MYSQL_DATABASE=main_db
MYSQL_USER=app_user
MYSQL_PASSWORD=app_password
MYSQL_PORT=3306

# WordPress Schema (Headless CMS)
WORDPRESS_DB_NAME=headless_cms

# Next.js App Schema (Business App)
NEXTJS_DB_NAME=business

# MinIO Configuration
MINIO_ROOT_USER=minioadmin
MINIO_ROOT_PASSWORD=minioadmin
MINIO_ENDPOINT=localhost
MINIO_PORT=9000
MINIO_CONSOLE_PORT=9001
MINIO_USE_SSL=false

# Application Configuration
NODE_ENV=development
NEXT_PUBLIC_APP_URL=http://localhost:3000

# WordPress Configuration
WORDPRESS_PORT=8000
PHPMYADMIN_PORT=8080